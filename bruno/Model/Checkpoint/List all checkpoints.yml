info:
  name: List all checkpoints
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/checkpoint"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
