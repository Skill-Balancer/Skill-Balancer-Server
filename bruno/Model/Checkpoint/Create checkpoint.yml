info:
  name: Create checkpoint
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/checkpoint/{{model_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
