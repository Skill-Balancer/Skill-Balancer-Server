info:
  name: Download export
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/models/export/{{model_id}}.safetensors"
  auth: inherit

runtime:
  variables:
    - name: model_id
      value: test

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
