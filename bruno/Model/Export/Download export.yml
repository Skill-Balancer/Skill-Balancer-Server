info:
  name: Download export
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/models/export/{{config}}/{{model_id}}.safetensors"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
