info:
  name: Create export
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/export/{{model_id}}"
  auth: inherit

runtime:
  variables:
    - name: model_id
      value: test

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
