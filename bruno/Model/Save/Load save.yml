info:
  name: Load save
  type: http
  seq: 4

http:
  method: GET
  url: "{{BASE_URL}}/load/{{name}}"
  auth: inherit

runtime:
  variables:
    - name: model_id
      value: test

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
