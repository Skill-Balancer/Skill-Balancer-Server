info:
  name: Load save
  type: http
  seq: 4

http:
  method: GET
  url: "{{BASE_URL}}/load/{{model_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
