info:
  name: Download save
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/models/save/{{model_id}}.mpk"
  auth: inherit

runtime:
  variables:
    - name: model_id
      value: test

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
