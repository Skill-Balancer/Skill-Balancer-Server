info:
  name: Download save
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/models/checkpoints/{{config}}/{{model_id}}.mpk"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
