info:
  name: Create save
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/save/{{model_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
