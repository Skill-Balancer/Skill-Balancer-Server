info:
  name: config setup
  type: http
  seq: 1

http:
  method: POST
  url: "{{BASE_URL}}/config"
  body:
    type: json
    data: |-
      {
        "name": "ikemen",
        "description": "THIS IS A TEST",
        "gamma": 0.99
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
