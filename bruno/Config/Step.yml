info:
  name: Step
  type: http
  seq: 3

http:
  method: POST
  url: "{{BASE_URL}}/step"
  params:
    - name: ""
      value: ""
      type: query
  body:
    type: json
    data: |-
      {
        "name": "ikemen",
        "game_state": [0.0, 0.0, 0.0, 0.0],
        "prev_reward": 0.0
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
