name: Build macOS Binary

on:
  pull_request:
    branches: [main, master]
  push:
    branches: [main, master]

jobs:
  build-macos:
    runs-on: windows-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Build
        run: cargo build --release

      - name: Upload to pull request
        uses: actions/upload-artifact@v4
        with:
          name: Skill-Balancer-Server-Windows.exe
          path: target/release/Skill-Balancer-Server.exe